# Public configuration for the Bike Taxi frontend
# The orchestrator will set real values in the environment; do not commit secrets.

# URL of the backend FastAPI root (no trailing slash), e.g. http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=

# Public site URL used for redirects and emails (optional), e.g. http://localhost:3000
NEXT_PUBLIC_SITE_URL=
